{
  "name": "ranked-list",
  "private": true,
  "version": "0.7.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "predeploy": "vue-tsc && vite build",
    "preview": "vite preview",
    "deploy": "echo list.cotterslist.com > dist/CNAME && cp serviceworker.js dist/ && cp manifest.json dist/ && gh-pages -d dist"
  },
  "dependencies": {
    "register-service-worker": "^1.7.2",
    "vue": "^3.3.4"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "@vitejs/plugin-vue": "^4.2.3",
    "gh-pages": "^6.1.1",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vue-tsc": "^1.8.5"
  }
}
